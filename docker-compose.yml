services:
  mongo:
    image: mongo:7
    container_name: rosterup-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - rosterup-mongo-data:/data/db

volumes:
  rosterup-mongo-data:
